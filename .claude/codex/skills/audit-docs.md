# Audit Docs

Codex alias for DevKey's existing `audit-config` workflow.

Source workflow: `.claude/skills/audit-config/SKILL.md`.

Use for `/audit-docs` or `audit docs`.

## Codex Wrapper

Run the same live-surface checks as `.codex/skills/audit-config.md`, with
emphasis on documentation drift and contradictory agent guidance.

Write the report to `.claude/outputs/audit-report-YYYY-MM-DD.md`.
