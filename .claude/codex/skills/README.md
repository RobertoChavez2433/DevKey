# Codex Skill Wrappers

These files are Codex-facing wrappers around the maintained workflows in
`.claude/skills/*/SKILL.md`.

Read the wrapper first, then load the matching `.claude` skill only when the
user's request triggers that workflow.
